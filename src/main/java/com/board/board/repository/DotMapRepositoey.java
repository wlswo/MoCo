package com.board.board.repository;

import com.board.board.domain.DotMap;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DotMapRepositoey extends JpaRepository<DotMap,Long> {
}
